# ---- Empacotamento / instalador (ffmpeg embutido + Setup gerado — pesado, não versionar) ----
/dist/

# ---- Build artifacts ----
*.exe
*.res
*.pdb
*.obj
*.o
*.lib
*.exp
*.ilk
*.bin

# ---- Backups ----
*.bak
*.old
*.old_*.exe

# ---- Log de diagnóstico do decoder (gerado por F4; específico da máquina) ----
decoder_log.txt

# ---- Saídas geradas pelo app (export/screenshot no save_dir padrão = pasta atual) ----
# Comente se quiser versionar alguma mídia de exemplo.
*.mp4
*.mov
*.mkv
*.png
!icon.png
*.jpg
*.jpeg
*.gif
# áudio temporário / extraído (normalmente vai pro TEMP do sistema, aqui por garantia)
*.wav
*.ogg
AUDIO_BASE_*
*_full.wav

# ---- Projetos salvos (descomente se NÃO quiser versionar .ovp) ----
# *.ovp

# ---- Outros logs gerados pelo app (o decoder_log.txt já está acima) ----
*.log

# ---- OS ----
Thumbs.db
Desktop.ini
.DS_Store

# ---- Editores / IDEs ----
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
